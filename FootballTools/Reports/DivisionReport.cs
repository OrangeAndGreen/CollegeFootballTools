using FootballTools.Entities;
using System.Collections.Generic;

namespace FootballTools.Reports
{
    public class DivisionReport
    {
        public static List<string> Generate(string conferenceName, string divisionName, League league)
        {
            //Division division = league.FindDivision(conferenceName, divisionName);

            List<string> ret = new List<string>();

            return ret;
        }
    }
}
